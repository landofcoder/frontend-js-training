import React from 'react';
import List from './List/List';


// const mapStateToProps = (state) =>({
//   state : state
// })


const Showlist = (/*state,remove*/) => (
  <List /*remove={todo.name} detail={todo.name} value={todo.date.toGMTString()}*/>
  </List>
)


export default
(Showlist);

